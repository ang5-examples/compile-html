export interface TagData {
  name: string;
  title: string;
  value: string;
}

export interface TagModifierHandler {
  onTagClick();
}

export interface DataTag {
  // name: string;
  title: string;
  // value: string;
}
